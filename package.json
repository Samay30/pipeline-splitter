{
  "name": "@eggers-internal/pipeline-sync",
  "version": "1.0.0",
  "private": true,
  "description": "Scheduled job that maintains slim *_RECAP.xlsx copies of recruiter pipeline workbooks on SharePoint so the Claude M365 connector can read them (masters exceed the connector renderer's extraction cap).",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "exceljs": "^4.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@vercel/node": "^5.1.0",
    "tsx": "^4.23.1",
    "typescript": "^5.7.0"
  }
}
